#!/usr/bin/env sh

# aquest script utiliza "mpc"

# ses dades són a partir de sa 2na columna
text=$(mpc current)

if [ -n "$text" ]; then
    printf '^c%s^ \xF0\x01$text' "$COL_B3"
else
    echo "^c$COL_BG_0^-"
fi
